# 全页面规范化检查

检查日期：2026-09-01 CST。基线：UI分支`codex/composable-ui-library-refactor`，`48a3465`加现有未提交改动。仅审查和登记，未修改UI、未提交Git。

## 覆盖口径

- 从`src/router/index.js`建立清单：14个实际页面路由；另检查`/redirect/:path(.*)`、根/父级跳转及404兜底。首页包含管理员/普通用户两种实现，内核页包含单机/批量模式。
- `src/views`共43个Vue文件已按页面及子面板纳入源码审查：结构、控件调用、样式、条件分支、加载/空值/错误及交互契约。并非逐行审计全部业务和后端安全。
- 本轮浏览器为618×918、本地Vite + mock API。12个页面路由有实屏/DOM检查；404和旧服务器详情没有本轮实屏，只完成源码及调用链检查。不能将“每个页面已有检查记录”写成“每个页面视觉全部通过”。
- 没有提交账号/节点/服务器增删改、上传文件、执行内核升级/回退/mTLS探测，也没有验证真实数据库。认证只使用本地mock示例输入，已恢复管理员演示并返回节点列表。

## 全路由覆盖表

以下源文件均相对`trojan-panel-ui/src`。每行源码检查均已完成，实屏范围单独列明。

| 页面路由 | 页面/子面板及源码范围 | 本轮实屏范围 | 遗漏/边界 |
| --- | --- | --- | --- |
| `/login` | `views/login`；品牌、验证码、显隐、注册入口、提交状态 | 页面、空值错误、mock登录成功返回首页 | WEB-031；失败响应未注入 |
| `/register` | `views/register`；两次密码、验证码、返回登录 | 页面及输入宽度、返回登录；未注册 | WEB-014同类错绑、031 |
| `/404` | `views/404.vue`；共享auth面板、品牌与返回首页 | 未实屏；源码检查完成 | 未发现新源码违规；不能替代实屏 |
| `/dashboard/index`（管理员） | `views/dashboard/index`、`admin/index`及两个流量表 | 指标、排行、全部/月/日切换、月份浮层、服务器流量表 | WEB-016选择组语义；完整日历键盘流程待验 |
| `/dashboard/index`（普通用户） | `user/index`；流量/到期/节点数、导出、排行 | 页面截图、辅助字色、查看节点入口 | WEB-026、030；复用导出受021影响 |
| `/modify/index` | `account/modify/index`、ModifyPass/ModifyProperty、PalettePicker | 顶部账号入口；密码、账号属性、颜色主题三个子页 | WEB-014、016；本轮未保存 |
| `/node-manage/node-list` | `node/list/index`及15个components文件，含协议表单和两处孤立组件 | 管理员/用户列表；详情Sheet；导出Dialog；新建表单的Xray、Trojan-Go、Hysteria、NaiveProxy、Hysteria2 | WEB-016、021–025；TLS/Reality/WebSocket/Fallback等深层配置为源码检查，未穷举每种协议组合 |
| `/account-manage/account-list` | `account/list/index`、BatchOperation；新建/编辑、批量、导入、分页 | 列表、新建、批量数量错误、导入弹窗、点击第2页 | WEB-017、019、022–024、027；未提交写入/下载任务 |
| `/server-manage/server-list` | `node-server/list/index`、NodeServerForm；额度、连接、证书、状态Sheet、导入 | 列表、状态Sheet、内核入口 | WEB-015–017、022–025、027、029；本轮表单深层连接/配额模式为源码检查，真实trafficStatus分支受mock限制 |
| `/server-manage/server-detail` | `node-server/detail/index`；旧Cookie取ID及指标面板 | 未实屏；源码/路由及引用检索完成 | WEB-025；无现有UI入口，仍注册为可访问路由 |
| `/server-manage/kernel-upgrade` | `kernel-upgrade/index`；单机/批量、通道、版本、回退、任务进度/明细/历史、重试、轮询 | 单机页、批量页、禁用按钮、空任务表、紧凑控件截图及尺寸 | WEB-024、028、029；有进度/失败/回退任务为源码检查，不执行升级 |
| `/emailManage/email-record` | `email/record/index`；搜索、状态筛选、表格、分页 | 过滤工具栏、空列表 | WEB-017、023、024；mock无邮件行，成功/失败行按源码检查 |
| `/taskManage/task-list` | `task-manage/list/index`；搜索、状态、下载、分页 | 过滤工具栏、空列表 | WEB-017、023、024；未创建或下载文件任务 |
| `/system/base-config` | `system/base/index`及account/email/web-file/panel-config/template-config | 五个子页；Clash.Meta、sing-box、Xray；模板选择框显隐及编辑区高度 | WEB-016、018、020、022、027、032；增删模板仍为预留接口，未触发上传/保存 |
| `/system/black-list` | `system/black/index`；搜索、空表、添加/删除、分页 | 空列表、添加Dialog字段与居中，取消关闭 | WEB-017、023、024；未添加/删除 |
| `/redirect/:path(.*)`及父路由 | `views/redirect/index`、router/permission | 非可视页面，源码检查 | 保留query的replace、根与父级redirect、404兜底已核对；未穷举所有跳转/权限组合 |

表格将同一路由的两种首页实现分列，因此行数不等于页面路由数量。WEB-022影响数值规则调用者，不意味着每个消费表单的页面都有数值字段错误。

## 子面板与共享层清点

- 认证/错误页：Login、Register、404；公共品牌与ThemeToggle。
- 个人资料：ModifyPass、ModifyProperty、PalettePicker。
- 首页：AdminDashboard、UserDashboard、TrafficTable、ServerTrafficTable，以及角色分派页。
- 节点：NodeForm、XrayForm及TLS/Reality/WebSocket、TrojanGoForm、HysteriaForm、Hysteria2Form、NaiveProxyForm、FallbackForm、FallbackInfo、NodeClientSelector、ExportNodeDialog；旧NodeDetail、NodeQrcode仅做引用与清理审查。
- 账号/服务器：BatchOperation、NodeServerForm、共享ImportTip；当前列表内联详情Sheet与旧独立服务器详情页分别检查，没有遗漏隐藏旧路由。
- 设置：五个子面板、ClientTemplateEditor、LiquidCodeEditor、UploadLogo。三编辑器在当前手机窗口均为260px、resize:none；sing-box有多模板下拉，单模板客户端不显示。
- 共享控件：Form/FormItem、Input、NumberInput、Select、DatePicker、Switch、Tag/EditTag、Table/Pagination；Dialog/Sheet、Confirm/Prompt、Loading、按钮/图标、布局导航、主题与Motion接入。已有测试通过不代表这些状态契约已全部正确。

## 本轮新增与扩展

新增11项：WEB-021～031。优先处理数字范围校验和分页同步，再处理弹窗几何、失败加载生命周期、上传状态与紧凑控件空间；之后清理旧详情、真实策略/日期展示、辅助文字和认证错误关联。

01:53补充：用户指出YAML没有格式化按钮，源码确认不仅按钮被json条件隐藏，格式化处理器也只支持JSON。上一轮检查了编辑器高度和缩放角，却漏记格式化能力不一致；补登记WEB-032，不将该编辑器视为完整验收通过。

01:55需求澄清：WEB-032要求YAML与JSON为平级语言能力，共享编辑器外壳及编辑/校验/格式化/错误/表单契约，各用对应语言处理器。WEB-028扩大为全局下拉框尾部图标留白规范，覆盖图标距右边框过远，不仅检查紧凑模式的文字截断；不新增重复编号。

既有项扩展：WEB-014包含注册页同类密码prop错绑；WEB-016包含导航current、周期分段状态和主题radio键盘契约。其余旧项保留，不重复新增编号。

证据、定位及建议见[代码审查](代码审查.md)最新章节；执行入口见[TODO](TODO.md)。

## 仍需专项验收的边界

- 360/390等更窄手机、完整平板/桌面断点、全部深浅主题的本轮逐页视觉矩阵；目前不是全尺寸通过。
- 404及旧服务器详情的实际导航；节点深层协议组合、多层弹窗键盘/焦点、完整日期键盘操作；真实数据的长内容与失败状态。
- mock契约需补齐：服务器trafficStatus/createTime、单机内核资料，以及导出格式枚举。mock导出返回`url`，真实接口支持`link/file/qrcode`，不把因此出现的翻译键当作已证实生产错误。
- `timeStampToDate(undefined)`的NaN已证实为前端缺省策略缺口，但此轮服务器缺字段由mock造成；不能推断生产接口总会缺值。
- 本轮自动门禁：Lint、19项UI清理测试、7个资源包检查、应用生产构建、两仓`git diff --check`通过。构建保留既有Sass弃用提示；新发现的行为需要新增测试，不能用现有绿色门禁关闭待办。

01:59补登记WEB-033：用户反馈账号属性三个框在平板/电脑端右边界不齐，要求各设备统一为手机端的“标签在上、字段在下”布局。同类表单项共用宽度上限、对齐和间距，长标签不挤压框体；本次仅记录，未重新实屏复现。

结论：全路由首轮审查完成，**Request Changes**。补登记后累计20项待修复（WEB-014～033），另保留WEB-010专项验收。没有未登记的页面路由，但仍有明确列出的实屏/数据状态验收范围。
